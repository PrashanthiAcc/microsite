import { NumericPlus } from './numeric-plus';

describe('NumericPlus', () => {
  it('should create an instance', () => {
    const directive = new NumericPlus();
    expect(directive).toBeTruthy();
  });
});
